import "./topbar.css";

function Topbar() {
    return (
        <nav className="Topbar">
            <button>Den Scuffed Avis</button>
            <button>Sign in</button>
        </nav>
    );
}

export default Topbar;
